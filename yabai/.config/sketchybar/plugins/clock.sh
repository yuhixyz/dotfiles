#!/usr/bin/env sh

sketchybar --set $NAME label="$(date '+%H:%M')"
# sketchybar --set $NAME label="$(date '+%I:%M %p')"
# sketchybar --set $NAME label="$(date '+%I:%M %p') -- $(TZ=Europe/London date '+%I:%M %p')"

